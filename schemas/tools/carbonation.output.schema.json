{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://werb.dev/schemas/tools/carbonation.output.schema.json",
  "title": "CarbonationOutput",
  "description": "Carbonation calculation results: residual CO2, sugar amounts for the three common priming sugars, and equivalent force-carbonation pressure.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "target_volumes_co2",
    "residual_volumes_co2",
    "volumes_to_add",
    "priming",
    "force_pressure_psi",
    "force_pressure_bar"
  ],
  "properties": {
    "target_volumes_co2": {
      "type": "number",
      "description": "Echoed target carbonation level."
    },
    "residual_volumes_co2": {
      "type": "number",
      "description": "CO2 already dissolved in the beer at package_temp_c, in volumes. Comes from fermentation byproducts; subtracted from the target to get the amount priming must add."
    },
    "volumes_to_add": {
      "type": "number",
      "description": "Volumes of CO2 priming must produce (target − residual). Negative if the beer is already over-carbonated for the target — in that case priming is not advised."
    },
    "priming": {
      "type": "object",
      "additionalProperties": false,
      "required": ["dextrose_g", "sucrose_g", "dme_g"],
      "properties": {
        "dextrose_g": {
          "type": "number",
          "description": "Grams of corn sugar (dextrose monohydrate) to prime with. Most common homebrew sugar; CO2 yield ≈ 0.46 g/g."
        },
        "sucrose_g": {
          "type": "number",
          "description": "Grams of table sugar (sucrose) to prime with. CO2 yield ≈ 0.51 g/g — about 10% less mass than dextrose for the same carbonation."
        },
        "dme_g": {
          "type": "number",
          "description": "Grams of dry malt extract to prime with. CO2 yield ≈ 0.34 g/g — about 35% more mass than dextrose since DME is only partly fermentable."
        }
      }
    },
    "force_pressure_psi": {
      "type": "number",
      "description": "CO2 regulator pressure in PSI to maintain target_volumes_co2 at serving_temp_c (Henry's law fit). For force carbonation in a keg."
    },
    "force_pressure_bar": {
      "type": "number",
      "description": "Same pressure expressed in bar."
    }
  }
}
