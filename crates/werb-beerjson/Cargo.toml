[package]
name = "werb-beerjson"
version = "0.1.0"
edition = "2021"
description = "BeerJSON 2.x types generated from the vendored JSON Schema via typify."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regress = "0.11"
